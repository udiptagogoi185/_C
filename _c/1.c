#include<stdio.h>

int main(){

    int L;
    int W;
    int Area;

        printf("Enter the Length :");
        scanf("%d",&L);

        printf("\tEnter the Width :");
        scanf("%d",&W);

    Area = L * W;
        printf("The area is = %d square unit",Area);

    return 0;
}